create table struct (
   id INT NOT NULL,
   citizen_id BIGINT NOT NULL,
   firstname VARCHAR(50) NOT NULL,
   lastname VARCHAR(50) NOT NULL,
   firstname_en VARCHAR(50) NOT NULL,
   lastname_en VARCHAR(50) NOT NULL,
   birthday DATE NOT NULL,
   PRIMARY KEY ( id )
);

INSERT INTO struct (id, citizen_id, firstname, lastname, firstname_en, lastname_en, birthday) VALUES (604019, md5(1409903101427), 'พลภณ', 'สุนทรภาส', 'Palapon', 'Soontronpas', '2002-03-11')

create table userdatabase (
   id INT NOT NULL,
   username VARCHAR(50) NOT NULL,
   password VARCHAR(50) NOT NULL,
   citizen_id BIGINT NOT NULL,
   firstname VARCHAR(50) NOT NULL,
   lastname VARCHAR(50) NOT NULL,
   email VARCHAR(50),
   PRIMARY KEY ( id )
);

create table post (
   id INT NOT NULL AUTO_INCREMENT,
   title LONGTEXT NOT NULL,
   writer LONGTEXT NOT NULL,
   time DATETIME,
   isHidden BOOLEAN,
   article LONGTEXT NOT NULL,
   cover LONGTEXT,
   PRIMARY KEY ( id )
);

create table profile (
   id INT NOT NULL,
   tel VARCHAR(20),
   email MEDIUMTEXT,
   greetings LONGTEXT,
   profile LONGTEXT,
   background LONGTEXT,
   PRIMARY KEY ( id )
);

INSERT INTO user (username, password, firstname, lastname, email) VALUES ('palapon2545', md5('P0ndJ@1103'), 'พลภณ', 'สุนทรภาส', 'palapon2545@gmail.com')

INSERT INTO `post` (title, writer, time, isHidden, article, cover) VALUES ('เด็กสาธิตเจ๋ง ป้ายกีฬาสีฯ ทำนายกฯ หน้าสั่น', 'RisakaLogin', 'YYYY-MM-DD HH:MM:SS', false, '', 'xxx.jpg')